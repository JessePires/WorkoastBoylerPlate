export enum CallStatusEnum {
  WAITING = 'WAITING',
  CONNECTING = 'CONNECTING',
  CONNECTED = 'CONNECTED',
}

export enum CallStatusEnumLabel {
  WAITING = 'callPage.transcriptionCard.person.interviewer',
  CONNECTING = 'callPage.transcriptionCard.person.interviewee',
  CONNECTED = 'callPage.transcriptionCard.person.interviewee',
}
